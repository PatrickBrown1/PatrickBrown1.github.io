import React from 'react';
import * as S from './HeroView.styles';

const HeroView = () => {
    return (
        <S.HeroView>
            Hero
        </S.HeroView>
    );
}

export default HeroView;